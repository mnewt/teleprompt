# function fish_prompt_theme
#   # Import theme from the bash version so we have only one place for most config
#   set theme (__prompt_import_theme "$HOME/.config/teleprompt/config/__prompt_theme")
#
#   for line in $bash_theme
#     eval $line
#   end
#   set -g __prompt_prompt '\n'(set_color -o)'> '(set_color normal)
# end
